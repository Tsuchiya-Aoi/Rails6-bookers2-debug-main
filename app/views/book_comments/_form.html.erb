<div>
  <%= form_with model:[book, book_comment], local: false do |f| %>
  <!--上記文中のlocal: false(非同期化)追記-->
    <%= f.text_area :comment, rows:'5', class: "w-100" %>
    <%= f.submit "送信" %>
  <% end %>
</div>